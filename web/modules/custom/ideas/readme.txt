To run the 'ideas_create_queue' queue individually, execute this from the Drupal project root:

drush queue:run ideas_create_queue

Ensure Drush is installed and you run the command where your site's settings.php are accessible.