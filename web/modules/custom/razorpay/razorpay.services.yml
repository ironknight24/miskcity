services:
  logger.channel.razorpay:
    parent: logger.channel_base
    arguments: ['razorpay']

  razorpay.auto_webhook:
    class: Drupal\razorpay\AutoWebhook
    arguments: ['@request_stack', '@messenger', '@config.factory', '@logger.channel.razorpay', '@string_translation']
