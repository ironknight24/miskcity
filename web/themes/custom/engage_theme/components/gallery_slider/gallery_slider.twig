{% if items %}
  <div class="swiper gallerySwiper w-full overflow-hidden mt-6">
    <div class="swiper-wrapper">
      {% for item in items %}
        <div class="swiper-slide">
          {{ item.content }}
        </div>
      {% endfor %}
    </div>
    <div class="swiper-button-next"></div>
    <div class="swiper-button-prev"></div>
    {# <div class="swiper-pagination"></div> #}
  </div>
{% endif %}