{# <div {{ attributes.addClass('mx-auto max-w-screen-xl topbar bg-white py-2 px-4') }}>
    <div class="cols-2">
    <div class="vission-row views-row clearfix row-1">
  {% block content %}
  <div class="vission-col views-col col-1">
    <h2 class="text-xl md:text-3xl font-700 mb-3 text-capitalize">{{ content.field_mission_title.0['#context'].value }}</h2>
    <div class="lg:text-lg text-base text-gray83 max-w-[100%] md:max-w-[90%] font-['Open_Sans'] opacity-80 leading-6 lg:leading-9">{{ content.field_mission_description.0['#context'].value }}</div>
    </div>
    <div class="vission-col views-col col-1">
    <h2 class="text-xl md:text-3xl font-700 mb-3 text-capitalize">{{ content.field_vision_title.0['#context'].value }}</h2>
    <div class="lg:text-lg text-base text-gray83 max-w-[100%] md:max-w-[90%] font-['Open_Sans'] opacity-80 leading-6 lg:leading-9">{{ content.field_vision_description.0['#context'].value }}</div>
  </div>
  {% endblock %}
  </div>
  </div>
</div> #}

<div {{ attributes.addClass('mx-auto max-w-screen-xl bg-white py-2 px-4') }}>
  {% block content %}
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
      <div class="vission-col">
        <h2 class="text-xl md:text-3xl mb-3 font-nevis capitalize">
          {{ content.field_mission_title.0['#context'].value }}
        </h2>
        <div class="text-base lg:text-lg text-gray83 font-sans opacity-80 leading-6 lg:leading-9">
          {{ content.field_mission_description.0['#context'].value }}
        </div>
      </div>

      <div class="vission-col">
        <h2 class="text-xl md:text-3xl mb-3 font-nevis capitalize">
          {{ content.field_vision_title.0['#context'].value }}
        </h2>
        <div class="text-base lg:text-lg text-gray83 font-sans opacity-80 leading-6 lg:leading-9">
          {{ content.field_vision_description.0['#context'].value }}
        </div>
      </div>
    </div>
  {% endblock %}
</div>
