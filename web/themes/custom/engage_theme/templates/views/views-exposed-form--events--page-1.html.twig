<div {{ attributes }}>
  <div class="events-list-page-exposed flex flex-col md:flex-row md:justify-between md:items-center gap-4 w-full mb-4">
    
    <!-- Left: Title -->
    <div class="w-full md:w-auto">
      {{ form.title }}
    </div>

    <!-- Right: Select list + Apply button -->
    <div class="flex flex-col md:flex-row md:items-center gap-6 w-full md:w-auto md:ml-auto">
      {{ form.field_event_categories_target_id }}
      {{ form.actions }}
    </div>

  </div>
</div>
